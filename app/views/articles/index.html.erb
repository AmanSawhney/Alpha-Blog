<h1 align="center"> 
	<%= "There #{@articles.size == 1 ? 'is' : 'are'} #{@articles.size} post#{'s' unless @articles.size == 1}." %> 
</h1>
<br>
<% if !@articles.any? %>
	<h2 align = "center" > Looks like there are no posts 
	</h2>
	<br>
	<p class= "center">
		<%= link_to "Create a article", new_article_path,class: 'btn btn-primary btn-lg' %>
	</p>
<% end %>
<%= render 'shared/list_articles', articlesobj: @articles %>
